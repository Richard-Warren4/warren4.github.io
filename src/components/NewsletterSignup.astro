---
---

<div class="newsletter-signup" id="newsletter">
  <div class="newsletter-content">
    <h3>Join the <span class="handwriting">fun</span> club 📬</h3>
    <p>
      Weekly-ish thoughts on building products & tinkering with AI. Zero spam, pinky promise. 🤙
    </p>
    <form
      action="https://buttondown.com/api/emails/embed-subscribe/warren4"
      method="post"
      class="newsletter-form"
      target="popupwindow"
    >
      <div class="form-group">
        <input
          type="email"
          name="email"
          id="bd-email"
          placeholder="your@email.com"
          required
          aria-label="Email address"
        />
        <input type="hidden" value="1" name="embed" />
        <button type="submit">Subscribe!</button>
      </div>
      <p class="powered-by">
        <a href="https://buttondown.com" target="_blank" rel="noopener noreferrer">
          Powered by Buttondown
        </a>
      </p>
    </form>
  </div>
</div>

<style>
  .newsletter-signup {
    margin: 3rem 0;
    padding: 2.5rem;
    background: var(--text);
    border-radius: 20px;
    position: relative;
    overflow: hidden;
  }

  .newsletter-signup::before {
    content: '📬';
    position: absolute;
    top: -20px;
    right: 30px;
    font-size: 5rem;
    opacity: 0.12;
    transform: rotate(12deg);
  }

  .newsletter-content {
    max-width: 500px;
    position: relative;
  }

  .newsletter-content h3 {
    margin: 0 0 0.5rem;
    color: white;
    font-size: 1.75rem;
  }

  .newsletter-content h3 .handwriting {
    color: var(--pop-3);
  }

  .newsletter-content p {
    margin: 0 0 1.5rem;
    color: rgba(255, 255, 255, 0.8);
    line-height: 1.6;
  }

  .newsletter-form {
    margin: 0;
  }

  .form-group {
    display: flex;
    gap: 0.75rem;
    margin-bottom: 0.75rem;
    flex-wrap: wrap;
  }

  .form-group input[type='email'] {
    flex: 1;
    min-width: 200px;
    padding: 0.875rem 1.25rem;
    border: 3px solid white;
    border-radius: 100px;
    font-size: 1rem;
    background: white;
    transition: all 0.2s ease;
    box-sizing: border-box;
    font-family: inherit;
  }

  .form-group input[type='email']:focus {
    outline: none;
    border-color: var(--pop-3);
    box-shadow: 0 0 0 4px rgba(255, 201, 92, 0.3);
  }

  .form-group button {
    padding: 0.875rem 2rem;
    background: var(--pop-3);
    color: var(--text);
    border: 3px solid var(--pop-3);
    border-radius: 100px;
    font-size: 1rem;
    font-weight: 700;
    cursor: pointer;
    transition: all 0.2s ease;
    white-space: nowrap;
    box-sizing: border-box;
    font-family: inherit;
    box-shadow: none;
  }

  .form-group button:hover {
    background: var(--pop);
    border-color: var(--pop);
    color: white;
    transform: scale(1.05);
    box-shadow: none;
  }

  .powered-by {
    margin: 0;
    font-size: 0.85rem;
    opacity: 0.6;
  }

  .powered-by a {
    color: rgba(255, 255, 255, 0.8);
    text-decoration: none;
    transition: opacity 0.2s ease;
  }

  .powered-by a:hover {
    opacity: 1;
    text-decoration: underline;
    color: white;
  }

  @media (max-width: 600px) {
    .newsletter-signup {
      padding: 2rem 1.5rem;
    }

    .newsletter-content h3 {
      font-size: 1.5rem;
    }

    .form-group {
      flex-direction: column;
    }

    .form-group input[type='email'],
    .form-group button {
      width: 100%;
      min-width: unset;
    }
  }
</style>
